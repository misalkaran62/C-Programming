#include<stdio.h>
 int main()
{
    int a,b;
    printf("ente 2 number");
    scanf("%d%d",&a,&b);
    //if else sysntax//
    if (a>b)
  printf("max is %d:a");
  else
  printf("max is %d:",b);


    return 0;
}
